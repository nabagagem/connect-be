keycloak:
  url: https://auth.ramifica.eu
  realm: master

spring:
  datasource:
    url: jdbc:postgresql://connect-db.cndzik9fvw56.us-east-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: ${DB_PASSWORD}
management:
  endpoint:
    health:
      show-details: always
